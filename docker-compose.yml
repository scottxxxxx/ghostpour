version: "3.8"
services:
  cloudzap:
    build: .
    container_name: cloudzap
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
