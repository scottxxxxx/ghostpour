{
 "status": 200,
 "content_type": "text/event-stream; charset=utf-8",
 "note": "Two sentence events, then the envelope holding the third as stream.held_sentence. The guard dropped a stale `asking`, so the envelope text differs from the model text."
}