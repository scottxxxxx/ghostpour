{
 "status": 200,
 "content_type": "text/event-stream; charset=utf-8",
 "note": "Non ASCII is sent as UTF-8, never \\u escaped (ensure_ascii=False), and the provider deltas here split multi byte characters across chunks."
}